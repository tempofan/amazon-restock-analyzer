# 亚马逊补货工具服务器配置文件 🚀
# 请根据实际情况修改以下配置

# ============= 服务器配置 =============
# 服务器IP地址（你的固定IP）
SERVER_HOST=127.0.0.1

# 服务器端口
SERVER_PORT=8000

# 调试模式（开发环境设置为true，生产环境设置为false）
DEBUG=false

# ============= 领星API配置 =============
# 领星API应用ID（请替换为你的实际应用ID）
LINGXING_APP_ID=ak_ogLvclRkg2uTq

# 领星API应用密钥（请替换为你的实际应用密钥）
LINGXING_APP_SECRET=S2Ufo0CpKeV4J9JwoTQ7wg==

# API请求超时时间（秒）
REQUEST_TIMEOUT=30

# 最大重试次数
MAX_RETRIES=3

# 重试延迟（秒）
RETRY_DELAY=1

# ============= 数据库配置 =============
# 数据库类型：sqlite, mysql, postgresql
DB_TYPE=sqlite

# MySQL配置（如果使用MySQL）
MYSQL_HOST=192.168.0.99
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=amazon_restock

# PostgreSQL配置（如果使用PostgreSQL）
POSTGRES_HOST=192.168.0.99
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DATABASE=amazon_restock

# ============= 日志配置 =============
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============= 文件存储配置 =============
# 输出目录
OUTPUT_DIR=output

# 数据目录
DATA_DIR=data

# 日志目录
LOG_DIR=logs

# 临时文件目录
TEMP_DIR=temp

# 最大文件大小（MB）
MAX_FILE_SIZE=100

# ============= 安全配置 =============
# 应用密钥（请更改为随机字符串）
SECRET_KEY=your-random-secret-key-here-change-this

# 请求频率限制
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# IP白名单（逗号分隔，空表示不限制）
ALLOWED_IPS=

# 会话超时时间（秒）
SESSION_TIMEOUT=3600

# ============= 飞书机器人配置 =============
# 飞书应用ID（在飞书开放平台获取）
FEISHU_APP_ID=cli_a8d49f76d7fbd00b

# 飞书应用密钥（在飞书开放平台获取）
FEISHU_APP_SECRET=ivQKKw0UsCHC2spYjrssvb0Hx4AdLxE6

# 飞书验证令牌（在飞书开放平台获取，可选）
FEISHU_VERIFICATION_TOKEN=AoM5n07qdPhIUNL0lliQwcpUOQJl5uGC

# 飞书加密密钥（在飞书开放平台获取，可选）
FEISHU_ENCRYPT_KEY=

# ============= 云代理配置 =============
# 是否启用代理模式（True/False）
ENABLE_PROXY=True

# 云服务器IP地址
PROXY_HOST=175.178.183.96

# 代理服务端口
PROXY_PORT=8080

# 代理协议（http或https）
PROXY_PROTOCOL=http

# 代理超时设置（秒）
PROXY_TIMEOUT=60

# 代理重试次数
PROXY_RETRIES=3

# 健康检查间隔（秒）
HEALTH_CHECK_INTERVAL=300

# ============= 其他配置 =============
# 时区设置
TIMEZONE=Asia/Shanghai

# 语言设置
LANGUAGE=zh-CN